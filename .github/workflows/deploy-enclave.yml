name: Deploy Enclave

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

# Required repo variables:
#   AWS_ROLE_ARN  - IAM role ARN with OIDC trust policy for this repo
#   AWS_REGION    - AWS region (e.g. us-east-1)

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: vars.AWS_ROLE_ARN != ''
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Install enclave CLI
        run: go install github.com/ArkLabsHQ/introspector-enclave/cmd/enclave@latest

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Install AWS CDK
        run: npm install -g aws-cdk

      # The enclave CLI doesn't auto-pull the Docker image before creating the container.
      - name: Pull Nix Docker image
        run: docker pull nixos/nix:2.24.9

      - name: Build and deploy
        run: |
          # Derive the AWS account ID from the OIDC role credentials and inject
          # it into enclave.yaml (kept empty in git to avoid leaking account info).
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          sed -i "s/^account: .*/account: \"${ACCOUNT_ID}\"/" enclave/enclave.yaml
          enclave build
          enclave deploy
